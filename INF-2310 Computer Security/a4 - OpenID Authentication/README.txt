IMPORTANT: The source code will fail to execute and is meant to be run on a VM together with letsencrypt and certbot. The source code also requires you to acquire an CLIENT SECRET generated by the Azure Portal and include it in app.py:37


The code in /src is the same as /a4-pre in the VM (ast283)

The app runs as a daemon using the systemd tool, and it utilizes HTTPS encryption.



The app should currently be running and is available on this URL:
	https://ast283.x310.net:5566



Commands for start/stopping the systemd service

	- Check the Status:
		sudo systemctl status app

	- Start the Service:
		sudo systemctl start app

	- Stop the Service:
		sudo systemctl stop app
